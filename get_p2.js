const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // wait, I don't know where the key is. Let's just check the firestore using the existing app code.
